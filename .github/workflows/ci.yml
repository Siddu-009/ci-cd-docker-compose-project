name: Runner Debug

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on:
      - self-hosted
      - Windows
      - X64

    steps:
      - name: Hello
        run: echo Hello

      - name: Hostname
        run: hostname

      - name: Current Directory
        run: pwd
